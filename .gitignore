# Large regenerable data artifacts (NEVER push)
# downloaded + extracted reth/geth snapshots (~321 GB)
/snapshots/
# migrated binary-trie RocksDB datadir (~33 GB)
/hoodi-binary-dd/
# geth datadir symlink used for exports
/hoodi-dd/
# geth db exports: preimages/snapshot/code (~39 GB) -- *.log under gethdump IS kept
/gethdump/**/*.rlp
*.tar.zst

# Cloned upstream repos (reference by URL + commit in README; do not vendor).
# No trailing slash: these are embedded git repos (gitlinks).
/ethrex
/go-ethereum

# Rust build output
**/target/

# Editor / OS junk
*.swp
*.swo
*~
.DS_Store

# --- Mainnet bootstrap data artifacts (added 2026-06-23; NEVER push) ---
/bn-datadir/
/mainnet-binary-dd/
/mainnet-dd/
/duckdb-tmp/
/duckdb
# Xatu: keep scripts (*.sh, *.py), drop all data
/xatu-preimages/xatu-raw/
/xatu-preimages/distinct/
/xatu-preimages/parts_addrs/
/xatu-preimages/parts_slots/
# belt-and-suspenders: no bulk data anywhere
*.rlp
*.parquet
*.bin

# Internal outreach/message drafts (never publish)
/planning/**/*outreach*
/docs/**/*outreach*

# xatu debug/data text dumps (regenerable)
/xatu-preimages/*.txt

# Session/run artifacts (kept on disk, not pushed)
/binary-node.log
/catchup-test.log
/reseed.log
/seedcode.log
/migrate-monitor.sh
/head_block_25340000.json
